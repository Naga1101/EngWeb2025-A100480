{
  "name": "cinema",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"json-server --watch json/db_cinema.json --port 5000\" \"node ./bin/www\"",
    "start server": "concurrently \"json-server --watch json/db_cinema.json --port 5000\" \"node app.js\""
  },
  "dependencies": {
    "axios": "^1.8.3",
    "cookie-parser": "^1.4.7",
    "debug": "^4.4.0",
    "express": "^4.21.2",
    "http-errors": "^2.0.0",
    "json-server": "^1.0.0-beta.3",
    "morgan": "^1.10.0",
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
